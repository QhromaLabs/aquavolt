import { Typography } from 'antd';
import MainLayout from '../../components/Layout/MainLayout';

const { Title, Paragraph } = Typography;

const SubmitFault = () => {
    return (
        <MainLayout>
            <Title level={2}>Submit Fault</Title>
            <Paragraph>Fault submission form coming soon...</Paragraph>
        </MainLayout>
    );
};

export default SubmitFault;
